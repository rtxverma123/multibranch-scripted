node{
    if (env.BRANCH_NAME == 'master'){
        stage('Build master'){
            echo 'Builing the code'
            
        }

    }

    if (env.BRANCH_NAME == 'deploy'){
        stage('Deploy code'){
            echo 'Deploying the code'
           

        }
    }
}
